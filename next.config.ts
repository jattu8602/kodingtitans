import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  /* config options here */
  output: 'standalone',
  // Uncomment when deploying to titanclean.in
  // basePath: '',
  // assetPrefix: 'https://titanclean.in',
}

export default nextConfig
